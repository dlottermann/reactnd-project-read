import React from "react";

const Nav = () => (
  <nav className="navbar">
        <a href='/' className="nav-item">Home</a>
        <a href='/' className="nav-item">New Post</a>  
  </nav>
);

export default Nav;
